# ABOUTME: Captures the bounded Gemma 2 9B model-scale sensitivity session that followed the mixed 2B refusal control.
# ABOUTME: Records the local feasibility result, the 9B refusal artifacts, and the new blocked state for the next session.

# Session 019

Date: 2026-03-18
Agent: codex-gpt5
Branch: `wip/creativedecomp-c4t-model-scale-sensitivity`

## Goal

Run `creativedecomp-c4t`: one bounded model-scale sensitivity check after the Gemma 2 `2B` refusal control stayed dense-output-weak.

## What Happened

1. Verified local feasibility for Gemma 2 `9B`

- Downloaded the checkpoint locally.
- Confirmed `google/gemma-2-9b` loads on `mps` in float32.

2. Ran the matched refusal layer sweep on Gemma 2 `9B`

- Artifact: `results/refusal_direction/20260318-gemma2-9b-layer-sweep-v1-mean-difference/`
- Result: layer `18` is the raw and controlled winner with `32 / 32` pair separation.

3. Tried the broader 9B sampled calibration grid

- Result: stopped intentionally.
- Reason: live process sampling showed the run spending most of its time in synchronous MPS `multinomial` and copy waits, which made the full grid a poor MacBook pilot use of time.

4. Replaced the broad calibration with a bounded output-level gate

- Artifact: `results/steering_eval/20260318-gemma2-9b-refusal-output-gate-v1-bounded/`
- Result: dense refusal steering at layer `18`, coeff `0.5` still fails the gate versus neutral.

## Net Result

- Gemma 2 `9B` is feasible locally for hidden-state work.
- Hidden-state refusal extraction gets even cleaner than on `2B`.
- The bounded 9B refusal gate still fails.
- Cross-scale takeaway: scale alone does not rescue dense output-level refusal steering on this stack.

## Next Session Start Point

- `creativedecomp-c4t` is complete.
- `creativedecomp-npt` remains blocked.
- Start with the cross-scale synthesis follow-up created after this session and decide whether the next honest continuation is a write-up-grade negative result, an instruction-tuned control lane, or a stop on dense base-model steering before decomposition.
