# Session 013 - 2026-03-18

## Goal

- execute `creativedecomp-173`
- compare the current PCA-on-pair-differences extraction against an Olson-style mean-difference or CAA-style extraction on the landed `v3` slice
- rereview the result against the research docs before changing the next-phase order

## Starting state

- `creativedecomp-173` is `in_progress`
- `creativedecomp-roq` and `creativedecomp-npt` remain blocked
- `v3` improved pair quality but still failed to recover a stable dense creativity direction

## Working notes

- prefer extending the existing layer-sweep path over creating a second competing sweep implementation
- if mean-difference materially improves the sweep, run bounded calibration on the recovered candidate band before changing the next gate

## Outcome

- added `mean_difference` extraction support to the existing sweep path instead of building a parallel implementation
- full-text `mean_difference` on the landed `v3` slice recovered layer `23` as a controlled winner with `0.580645` positive-greater-than-negative fraction and mean margin `58.126850`
- response-only `mean_difference` recovered layer `9` with `0.548387` fraction and mean margin `13.131884`
- full-text `mean_difference` calibration is substantially cleaner than the failed PCA calibration: layer `23` improves from `15.270608` unsteered to `26.416735` at coeff `0.5` and `34.472979` at coeff `1.0`, then overshoots at `2.0`
- response-only `mean_difference` improves over PCA but remains weaker and more view-dependent

## Secondary research review

- this result aligns with the strongest reading of [experiment-novelty.md](/Users/sohailmo/creativedecomp/research/experiment-novelty.md): the last major mismatch to Olson was not only pair construction, but extraction method
- the new artifacts support reopening the pilot output gate on the full-text `mean_difference` path while keeping decomposition blocked
- the repo should not claim the extraction problem is fully solved across views; the response-only path is still weaker, which keeps the Phase 1 story honest
