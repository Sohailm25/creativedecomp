# ABOUTME: Session log for the matched refusal control and the resulting shift toward model-scale sensitivity.
# ABOUTME: Captures the key refusal artifacts, judge-path fixes, and the updated interpretation of the 2B lane.

# Session 018

Date: 2026-03-18
Agent: codex-gpt5

## Goal

Run `creativedecomp-rcf`: a matched refusal baseline on the same Gemma 2 `2B` stack before any model-scale escalation.

## Planned Work

1. Freeze a refusal pilot pack and refusal-specific prompt/judge templates.
2. Reuse the same-stack layer-sweep, calibration, and output-gate pattern.
3. Decide whether the refusal control strengthens the creativity-negative interpretation or instead points to broader stack weakness.

## Outcome

- Added a refusal pilot pack with `32` response-centered refusal/compliance pairs and refusal-specific generation/judge templates.
- Landed refusal-specific sweep, calibration, and output-gate scripts while fixing two shared judge bugs:
  - force at least one new judge token
  - conservatively treat unparseable judge outputs as `tie`
- The refusal layer sweep recovered a very clean hidden-state direction at layer `15`.
- The output-level refusal gate did not become a clean dense success case. The prefixed-refusal harness only weakly recovered the prompt-only refusal baseline, while dense refusal steering still failed the gate.
- Updated the repo interpretation: the Gemma 2 `2B` lane now looks broadly weak for dense output-level steering, so bounded model-scale sensitivity is the next lane and decomposition remains blocked.
