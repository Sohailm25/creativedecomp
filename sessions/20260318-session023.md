# ABOUTME: Captures the bounded instruction-tuned refusal pivot on Gemma plus GemmaScope v2.
# ABOUTME: Records why the repo now treats the local steering lane as exhausted enough for a write-up-grade negative result.

# Session 023

Date: 2026-03-18
Agent: codex-gpt5
Branch: `wip/creativedecomp-177-instruction-tuned-refusal-pivot`

## Goal

Run `creativedecomp-177`: execute one bounded instruction-tuned refusal pivot before finalizing the local steering lane as negative-result territory.

## What Happened

1. Froze the smallest locally feasible instruction-tuned Gemma plus SAE stack

- Confirmed `google/gemma-3-270m-it` loads on `mps`, supports chat generation, and works with hidden-state extraction.
- Confirmed `gemma-scope-2-270m-it-res` is the correct `sae-lens` release alias locally.
- Froze `layer_12_width_16k_l0_medium` as the reference SAE and residual candidate layers `[5, 9, 12, 15]`.

2. Landed the bounded implementation cleanly

- Added `scripts/run_instruction_tuned_refusal_pivot.py`.
- Added `prompts/refusal_direction_it_v1_templates.json`.
- Added `tests/test_instruction_tuned_refusal_pivot.py`.
- Ran the smoke slice first, fixed the deterministic judge-generation warning, then launched the full run.

3. Ran the full instruction-tuned refusal sweep and gate

- The hidden-state sweep under `results/refusal_direction/20260318-gemma3-270m-it-layer-sweep-v1-mean-difference/` recovered a perfect refusal direction at layer `9`.
- The output gate under `results/steering_eval/20260318-gemma3-270m-it-refusal-output-gate-v1/` still collapsed to refusal ties for prompt-only and dense conditions versus neutral.
- Checked raw outputs before interpretation to rule out another obvious evaluation artifact.

## Net Result

- `creativedecomp-177` is resolved as a failed rescue.
- The last allowed steering-regime pivot did not recover a clean simpler-concept output effect.
- The repo should now move to a write-up-grade negative-result synthesis instead of more steering-regime shopping.

## Next Session Start Point

- Start `creativedecomp-178`.
- Synthesize the dense, sparse, cross-scale, and instruction-tuned evidence into a durable negative-result framing and evidence bundle.
- Keep `creativedecomp-npt` blocked unless that synthesis identifies a concrete paper-backed reason to reopen the lane.
