# Session 012 - 2026-03-18

## Goal

- execute `creativedecomp-02c`
- replace the weak `v2` independently sampled contrast with a tighter `v3` counterpart-style contrast
- perform a secondary research review after the `v3` artifacts land

## Starting state

- `creativedecomp-02c` is now `in_progress`
- `creativedecomp-roq` and `creativedecomp-npt` remain blocked
- the local corpus review says `v2` is too weak because positive and negative rows differ in content quality, not just creativity level

## Working notes

- likely minimal `v3` path: creative source continuation plus plain content-preserving rewrite
- filter with overlap and prompt-grounding heuristics derived from the existing audit slice
- rerun layer sweep and bounded calibration after the new pair set lands

## Outcome

- `v3` landed as a real methodological improvement: `31 / 32` rows accepted with mean counterpart overlap `0.913658` and mean prompt-grounding delta `0.002251`
- the improved contrast did not recover a stable dense creativity direction
- the full-text sweep failed with raw best layer `6` at `0.354839` positive-greater-than-negative fraction and no controlled winner
- the response-only sweep moved the raw best layer to `22`, but it still failed with `0.387097` fraction and no controlled winner
- both calibration branches remained non-monotone and view-dependent

## Secondary research review

- alignment improved materially with `research/experiment-novelty.md` and the Olson-style critique because the pair set now uses content-preserving counterparts rather than independently sampled negatives
- decomposition remains correctly blocked; the results still support the paper-level warning from Von Rütte that creativity-like concepts can be detectable yet hard to steer cleanly
- the main remaining research mismatch is now extraction method, not pair construction alone: Olson uses a mean-difference or CAA-style direction on high-quality counterpart data, while the current implementation still relies on PCA-on-pair-differences
- the honest next step is therefore one bounded Olson-style extraction sensitivity before the repo either opens the output gate or writes up a serious negative result for the Gemma 2 2B MacBook lane
